#include <iostream>

int main()
{
  std::cout << "Hello World!\n"; // this std:: is just saying that to look for cout in std library
                                 // anything with \ char before is a special character
                                 // "<<" this is "put to" that takes the second argument and puts it in first character
                                 // brian says to use namespace instead of std to make the code work without specific std::
}
